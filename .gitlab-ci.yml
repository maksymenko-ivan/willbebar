image: node:20

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - rm -rf public
    - mv dist public
    # SPA fallback (useful if you use React Router)
    - cp public/index.html public/404.html
  artifacts:
    paths:
      - public
  only:
    - main